[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
tokio = { version = "1.35", features = ["full"] }
anyhow = "1.0"
[dependencies]
common = { path = "../common" }
